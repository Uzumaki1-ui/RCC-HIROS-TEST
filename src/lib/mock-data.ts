// STUB: This file provides minimal placeholder data for components not yet migrated to real API data.
// As components are migrated, these stubs will be removed.

export const mockPolls = { active: [] as unknown[], closed: [] as unknown[] };
export const mockEvaluations = [] as unknown[];
export const mockCertificates = [] as unknown[];
export const mockMoodHistory = [] as unknown[];
export const mockGamification = { monthlyPoints: 0, lifetimePoints: 0, breakdown: [] as { action: string; points: number; count: number; total: number }[] };
export const mockLeaderboard = [] as { rank: number; name: string; department: string; monthlyPoints: number; lifetimePoints: number; isCurrentUser?: boolean }[];
export const mockAttendance = [] as unknown[];
export const mockLeaveBalances = [] as unknown[];
export const mockLeaveRequests = [] as unknown[];
